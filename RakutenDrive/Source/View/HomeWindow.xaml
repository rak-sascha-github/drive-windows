<Window x:Class="RakutenDrive.HomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
        xmlns:local="clr-namespace:RakutenDrive"
        mc:Ignorable="d"
        Title="Rakuten Drive" Height="450" Width="472"
        ShowInTaskbar="True"
        ResizeMode="NoResize"
        SizeToContent="Manual"
        MinWidth="472" MinHeight="450"
        Icon="/Assets/Images/Icons/Logo_light.ico"
        >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <!-- New RowDefinition for the button -->
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="36,0,0,0">
            <!-- Circular Image on the left -->
            <Ellipse x:Name="myEllipse" Width="40" Height="40" Margin="10">
                <Ellipse.Fill>
                    <VisualBrush x:Name="skeletonBrush">
                        <VisualBrush.Visual>
                            <!-- Define your skeleton or placeholder visual here -->
                            <TextBlock x:Name="textProfile" FontSize="9" LineHeight="9" Width="20" Height="20" Background="LightGray" Padding="0 3 0 0" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Ellipse.Fill>
            </Ellipse>
            <!-- StackPanel for text blocks -->
            <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
                <TextBlock FontFamily="Rakuten Sans UI" Text="N/A" FontSize="14" FontWeight="Bold" Name="displayNameLabel"/>
                <TextBlock Text="N/A" FontSize="14" Name="displayEmailLabel"/>
            </StackPanel>
        </StackPanel>

        <!-- New StackPanel added in the new row -->
        <StackPanel Grid.Column="0" Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="36,0,0,0" Height="74">
            <svg:SvgViewbox Source="pack://application:,,,/Assets/Images/rakuten_drive_folder_image.svg" Width="354" Height="74" Grid.Column="0" VerticalAlignment="Center" MouseLeftButtonUp="OnOpenFolderMouseLeftButtonUp" Cursor="Hand"/>
        </StackPanel>

        <StackPanel Grid.Column="0" Grid.Row="5" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="36,0,0,0">
            <Border Background="#E9ECEF" Padding="0.5" CornerRadius="5" Width="354">
            </Border>
        </StackPanel>
       
        <StackPanel Grid.Column="0" Grid.Row="6" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="left" Margin="36,0,0,0">
            <svg:SvgViewbox Source="pack://application:,,,/Assets/Images/button_Logout.svg" Width="109" Height="42" Grid.Column="0" VerticalAlignment="Center" MouseLeftButtonUp="OnLogOutButtonClicked" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>

    
    